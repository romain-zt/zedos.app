# PRD Open Questions

## Active queue

| ID | Status | Priority | Question | Source note | Blocks |
|---|---|---:|---|---|---|
| Q-021 | open | 3 | Is email verification required before a founder's first access to the signed-in web app after signup? If yes, a "check your inbox" intermediate state must be added and the sign-up flow becomes two-step. If no, signup leads directly to the signed-in dashboard. | FA-account-session redefinition (2026-05-10) | `account-session--sign-up-sign-in` UX states; slice promotion to `ready-for-user-stories` |
| Q-022 | open | 2 | What is the expected product behavior when a founder's session expires during an active clarification or PRD editing session — is unsaved in-progress work preserved (requiring auto-save or draft recovery across FAs), or is data loss on expiry acceptable for v0? | FA-account-session redefinition (2026-05-10) | `account-session--session-persistence-protected-routes` acceptance outcome; cross-FA dependencies (PRD versioning, guided clarification) |
| Q-023 | open | 4 | Is profile editing (display name, password change, email change) a v0 requirement for the account-session Feature Area, or is it deferred to a future release? If required, a third scope slice must be defined for this FA. | FA-account-session redefinition (2026-05-10) | FA-account-session completeness; whether a third scope slice is needed |

## Answered

| ID | Answered at | Question | Answer | PRD impact |
|---|---|---|---|---|
| Q-001 | 2026-05-09 | What core problem are you solving, and who is the first user or segment you want to serve? | **Users:** founders who use AI to ship and validate a market idea, often starting from a vague concept. **Problem:** need structured accompaniment from vague idea → product clarification → versioned PRD; long-term vision continues into services/feature groups, technical analysis, architecture/Cursor setup, user stories, and test-first delivery, but **v0 intentionally ships only the PRD slice**; other areas show as “under construction” in the customer dashboard. | Anchors Problem & Users and clarifies v0 scope cut (PRD path in scope; rest deferred in the product narrative). Persist via `/prd converge` → `/prd update`. |
| Q-002 | 2026-05-09 | For v0 (PRD-only), where does the founder primarily work on clarification and the versioned PRD—browser/dashboard only, Cursor/IDE-integrated flow, or a defined hybrid—and what is the single default “approval” action in that experience (e.g. explicit approve button, structured chat command)? | **Primary work surface:** web app—migrate the current Cursor PRD flow into an online product with intentional UX/UI. **Navigation:** users must easily switch between **multiple PRDs across different projects** and between **multiple PRD versions within one project**. **Hybrid with Cursor:** not positioned as the v0 primary path here (may remain optional later). **Approval action:** not specified in this input; tracked under Q-003. | Buyer-facing surface = web; implies business objects for Project vs PRD version; informs Core User Journeys (project switcher + version/history navigation). Persist via `/prd converge` → `/prd update`. |
| Q-003 | 2026-05-09 | How does a user move the PRD workflow forward in the web app—what is the default “approve” or “accept” interaction for a clarification step or PRD version (e.g. primary button + optional comment, review modal, async review link), and is any confirmation required outside the app (email, etc.)? | **In-app first:** use obvious in-product controls—buttons, inputs, or similarly easy interactions—to advance or approve; v0 does not rely on out-of-app confirmation as the default path. Exact control pattern (button vs inline input vs modal) can remain a UX detail. | Sets `Confirmation channel` to in-app-first; journeys should include explicit continue/approve affordances; external email/link confirmation not a v0 default. Persist via `/prd converge` → `/prd update`. |
| Q-004 | 2026-05-09 | For v0, what **language/locale** should the PRD experience (UI + AI) optimize for first—single language only, or multiple from day one? | **English only** for the first release (UI + AI). **French** is expected next—plan product and UX so **localization can land** (i18n-ready content/strategy) without blocking v0 on multi-language UI. | Locks **Primary market / language** to EN-first; documents a phased roadmap to FR; informs Configuration Matrix / MVP (i18n readiness vs FR scope). Persist via `/prd converge` → `/prd update`. |
| Q-005 | 2026-05-09 | For v0, how does a **new founder** first get into Zedos (buyer entry)—e.g. public signup, waitlist, invite-only beta, or another gate? | **Public signup** — open, self-serve registration as the v0 default (not waitlist- or invite-gated). | Sets **Buyer entry point** to public acquisition; onboarding/auth journeys assume standard signup. Persist via `/prd converge` → `/prd update`. |
| Q-006 | 2026-05-09 | For v0, is the **in-app versioned PRD the single source of truth**, or must **export/download** (e.g. Markdown/PDF) be a must-have when a PRD version is considered “complete”? | **In-app versioned PRD is the source of truth.** Export is **not** required for a version to count as “done.” **v0 focus:** guided clarification, **versioned PRD state**, **question history**, and **shareable/readable output inside the app** (view/link semantics). **Markdown export:** planned as **fast follow** or lightweight **v0.1**—common need for stakeholder alignment outside the tool. **PDF export:** **not v0-critical** (defer). | Pins **Source of truth** to the product database/UI; separates MVP “complete” from file export; phases export MD vs PDF; informs MVP checklist and sharing journeys. Persist via `/prd converge` → `/prd update`. |
| Q-007 | 2026-05-09 | For v0 (PRD-only), what is the **monetization intent**—free while iterating, freemium, paid subscription, usage/credits, or explicit “decide later” with a **default assumption** you want the PRD to use for scope tradeoffs? | **Free to start** with **signup credits (X)** to create/iterate a first PRD. **AI usage is prepaid credits** consumed **per AI operation**. **When credits = 0**, **generation is blocked** until **purchase more credits**. **No subscription in v0.** **No BYOK in v0.** **No unlimited free AI.** **Positioning:** “Start free with included credits. Buy more only when you need them.” | Defines **Payment model** (prepaid credits + starter grant); monetization exclusions (subs/BYOK/unlimited-free); implies billing/top-up + balance UX; informs Success Metrics (credit purchase vs sub). Persist via `/prd converge` → `/prd update`. **Narrowed by Q-014:** during the **one-time first PRD circuit**, a **small overage** may **complete the in-flight AI response** before enforcement/recharge; afterward, **strict balance rules** apply **unless auto-reload**. **SUPERSEDED (credit mechanics) by Q-018/Q-019/Q-020 (2026-05-09):** burn tier model, 20-credit grace ceiling + pre-check gate, and auto-reload SCA fallback now govern; governing text: `docs/prd/PRD.md`. |
| Q-008 | 2026-05-09 | Should the **free signup credit grant (X)** be a **fixed number or range in the PRD** for v0 planning, or stay **explicitly TBD / tunable** until you model cost per AI operation? | **Stay TBD** — treat **X as configuration/ops tuning**, not a fixed number committed in the PRD; “doesn’t matter” for product narrative beyond “non-zero starter grant exists.” | **Configuration Matrix** carries X as tunable; avoids false precision in `PRD.md`; still compatible with credit-based model in Q-007. Persist via `/prd converge` → `/prd update`. |
| Q-009 | 2026-05-09 | In v0, is a project/PRD **strictly single-user**, or should **inviting collaborators** (view/edit roles) be in scope for alignment workflows? | **Single-user for v0** — no multi-user collaboration/invites/roles in scope for the first release. | **Operating Model** = solo founder; simplifies auth/permissions MVP; sharing stays **in-app readable/link** semantics from Q-006 rather than co-editing. Persist via `/prd converge` → `/prd update`. |
| Q-010 | 2026-05-09 | What **2–3 concrete metrics or outcomes** will define **v0 success** (e.g. signup → first PRD version, weekly return, credit purchase rate), beyond “the workflow exists”? | **Not known yet** — founder has **not selected success metrics**; this remains an **explicit open product decision** (not a hidden default). | **Success Metrics** in `PRD.md` must stay **TBD** or explicitly “unchosen”; **ICE/prioritize** should treat **Confidence as limited** until metrics are defined; no pretending v0 success is measurable without a chosen bar. Persist via `/prd converge` → `/prd update`. **SUPERSEDED by Q-013 (2026-05-09):** v0 metrics now defined (feedback-led); keep this row for history only. |
| Q-011 | 2026-05-09 | For **shareable / readable in-app PRD output** in v0, should **viewers need an account (sign-in)**, or is **unauthenticated read** (e.g. read-only link without login) required or explicitly out of scope? | **In scope for v0:** **unauthenticated read-only share links.** **Owner** (signed-in) generates a link; **viewers** read **without signing in**. Viewers **cannot** edit, comment, duplicate, or access **workspace/private history.** **Defaults:** owner/editor **signed in**; viewer **via link**; owner can **disable** link; **public share URL not search-indexed** (noindex as product requirement). | Drives **Core User Journeys** (mint/read/revoke), **privacy** boundaries for shared vs private surfaces, and **MVP** sharing scope; clarifies anon viewer permissions. Persist via `/prd converge` → `/prd update`. |
| Q-012 | 2026-05-09 | Beyond **owner disable** and **noindex**, should v0 include **time-limited links** and/or **password-protected links**, or is **disable-only** sufficient for the first release? | **v0 stays intentionally simple:** **public read-only links only** — **no** password, **no** expiry, **no** additional private-link controls beyond what’s already decided (owner disable + noindex). **Advanced sharing** may become a **paid feature later** if traction warrants; **not v0**. | Locks **MVP sharing** to the minimal control set; defers password/expiry/extra controls; records optional future **monetization** angle for advanced sharing (hypothesis). Persist via `/prd converge` → `/prd update`. |
| Q-013 | 2026-05-09 | Following Q-010 (metrics still unset in PRD), what **2–3 concrete v0 success metrics** will you adopt—each with a **plain-language definition** and why it matters for Zedos (working hypothesis if labeled as such)? | **Primary:** **user feedback quality** after PRD **generation / iteration**. **Capture UX:** after **key moments only** (first PRD version generated; PRD updated after clarification; share link created / PRD viewed)—**automatic** prompt, **lightweight** and **skippable**; **1–5 stars** or **like/dislike**; **optional** comment. **Store** with **project**, **PRD version**, **workflow step**. **Working metrics:** (1) **Feedback response rate** — % who respond to the prompt after a key step (learn whether signals are usable). (2) **Positive feedback rate** — % of submissions that are **4–5** or **like** (usefulness signal). (3) **Negative feedback reasons** — **themes** from **1–3** / **dislike** comments (prioritize fixes: clarity, questions, structure, generic output, missing context, etc.). | **Success Metrics** + **Surface Blockers** can move from “unchosen” to **feedback-led definitions**; add **journeys/flows** for milestone prompts; **business objects** likely include **feedback** tied to version/step; **anonymous “PRD viewed”** vs **owner** may need boundary clarity later. Persist via `/prd converge` → `/prd update`. **Refined after `/prd converge` (user checkpoint, 2026-05-09):** v0 prompts **signed-in owner only**; **no** feedback prompts for **anonymous share** viewers (**deferred post-v0**). Milestones: **first PRD version created**; **PRD version updated after clarification**; **PRD shared**; **PRD reopened / viewed by owner after generation**. Attribution: **project**, **PRD version**, **milestone**, **timestamp**. **Governing product text:** `docs/prd/PRD.md` after approved `/prd update`. |
| Q-014 | 2026-05-09 | For v0 **credit top-ups**, what is the intended **commercial shape**: e.g. one simple pack vs a small menu of packs, approximate **price bands** (even rough), and any **geo/currency** constraints you care about on day one? | **First-run grace (one-time, first PRD circuit):** signup gets **X starter credits**. If user **slightly exceeds** balance during that circuit, **current AI response completes** (no mid-stream cut). Afterward, show explanatory copy: exceeded included starter credits by **Y**; **covered once** so first PRD flow wasn’t interrupted. Then **recharge modal**: **buy credits now**, **enable auto-reload**, or **continue without buying** ( **paid AI blocked** until recharge). **After** first circuit: **normal debiting**; **block generation** when balance insufficient **unless auto-reload** covers it. *(Pack sizes, list prices, geo/currency constraints not specified in this answer — see Q-016.)* | **Credits journey** gains **grace policy**, **first-circuit scope**, **post-grace strictness**, **auto-reload** as user-facing option, and **recharge UX**; may interact with **Q-008** (X still tunable) and billing edge cases (implicit one-time courtesy overage). **Stripe + FR/EU + US launch** specified in **Q-015**. Persist via `/prd converge` → `/prd update`. **SUPERSEDED (grace cap + auto-reload) by Q-019/Q-020 (2026-05-09):** 20-credit fixed ceiling with pre-check gate; auto-reload best-effort with SCA fallback; governing text: `docs/prd/PRD.md`. |
| Q-015 | 2026-05-09 | For v0, should the PRD state any **non-negotiable payment or compliance** constraints (regions, methods, tax posture) that would **exclude** certain providers—or stay deliberately provider-agnostic until engineering? | **Provider:** **Stripe** for v0. **Launch regions:** **France/EU + US**; must support **payment flows** in those markets. **Product requirements:** **prepaid credit packs**; **one-time top-ups**; **packs = X credits** (not “1 PRD / N PRDs”); **internal credit ledger** owned by Zedos; **deduct per AI operation**; **saved card** for future **auto-reload**; **auto-reload opt-in only**, **not** hidden subscription—**prepaid refill rule** only. **Tax:** **clear VAT/tax handling** for **digital AI credits**. **Reiterates credits policy:** first circuit **X** starter + **one-time slight overage** completes current response then offer coverage once; **after grace**, **paid AI blocked at zero** unless **auto-reload succeeds**. **No BYOK**, **no subscription**, **no unlimited free AI** in v0. | **Integration Boundaries** can lock **Stripe** + **FR/EU/US** + **tax posture**; **Configuration Matrix** aligns **packs-in-credits**; reinforces **Q-007/Q-014** (auto-reload ≠ subscription). Persist via `/prd converge` → `/prd update`. |
| Q-016 | 2026-05-09 | For v0 **credit packs** sold via **Stripe**, should Zedos ship **one pack SKU**, a **small menu** (e.g. 2–4 tiers), or **operator-config only**—and what **rough price bands** (directional **EUR/USD**) feel right for launch? | **Pack sizes (fixed):** **100**, **200**, and **1000 credits**—always sold as **prepaid credit quantity**, never as “number of PRDs.” **Consumption:** **progressive per AI operation** (restated). **Pricing:** **operator-configurable**; may **differ by currency/region**. **Manual top-up:** **Stripe one-time payments**. **Auto-reload (opt-in):** purchases **one of these same three packs** using a **saved payment method**; **does not** create a **subscription**. *(No fixed EUR/USD price bands in product narrative—ops tuning.)* | **Configuration Matrix** can enumerate **100 / 200 / 1000** while keeping **prices regional/config**; **journeys** cover **checkout** vs **auto-reload pack selection**. Persist via `/prd converge` → `/prd update`. |

| Q-017 | 2026-05-09 | What is the UX interaction model for the guided clarification loop — chat-style, structured questionnaire, or hybrid? | **Chat-driven dynamic decision UI.** Chat is the guidance and reasoning layer; when a product decision benefits from constrained input, Zedos **generates a contextual mini-form on the fly** (modal with select fields, single-choice decision card, multi-choice checklist, ranked options, "not sure / ask me differently" action). Not a free-form chatbot; not a static questionnaire. **Question history** is a structured log per decision: structured question, **available options**, founder answer, optional comment, AI interpretation, PRD impact (6 fields). | Clarification loop of FG-PRD-V0 defined as chat-driven dynamic decision UI. Question history business object gains 6 required fields (including available options + optional comment). Governing product text: `docs/prd/PRD.md` after approved `/prd update`. |
| Q-020 | 2026-05-09 | Should auto-reload be deferred to v0.1 given EU SCA complexity, or is it essential for v0 monetization conversion? (Raised by `/prd challenge`.) | **Stays in v0 as best-effort opt-in convenience layer.** Manual top-up is primary path. Auto-reload: opt-in, buys one prepaid pack via saved PM, not a subscription. If auto-reload succeeds → credits added, generation continues. If auto-reload fails or requires authentication → fall back to manual recharge UX (clear message, manual confirm/authenticate). Generation remains blocked until recharge succeeds. Hard invariants: no hidden debt, no silent retry loop, no negative balance except first-circuit grace. Rationale: EU/SCA may interrupt off-session payments; v0 handles this gracefully rather than avoiding the feature. | Auto-reload description in PRD.md must change to "best-effort convenience with SCA fallback." New product behavior: auto-reload failure → manual recharge UX fallback. EU/SCA FALSE CONVERGENCE RISK from challenge is resolved at product level. Risks & Assumptions gains SCA note. Hard invariants are product-level constraints to persist. Persist via `/prd converge` → `/prd update`. |
| Q-019 | 2026-05-09 | What defines "slightly exceeds" in the first-circuit grace policy — percentage, fixed ceiling, or operator-configurable? (Raised by `/prd challenge`.) | **Fixed credit ceiling: at most 20 extra credits.** Applies once, first PRD circuit only, to let the current in-flight response finish. Pre-check gate: if projected overage > 20 credits, do not start the operation — block it. After grace fires: recharge modal. Post-first-circuit: no grace; paid AI blocked at zero unless auto-reload succeeds. Rationale: 20 credits avoids a bad first-use interruption without becoming an exploitable free tier. | "Slightly exceeds" language in PRD.md should be replaced with explicit 20-credit ceiling + pre-check gate. Credit system flow gains a precise grace rule and a new pre-check behavior. Anti-abuse rationale worth noting in Risks & Assumptions. Persist via `/prd converge` → `/prd update`. |
| Q-018 | 2026-05-09 | What is the directional credit consumption range per AI operation — needed to evaluate whether 100/200/1000 packs map to recognizable founder value? (Raised by `/prd challenge`.) | **Directional burn model (product assumption, not final pricing):** Lightweight clarification step = 1 credit; Standard decision / clarification step = 3 credits; Dynamic mini-form decision step = 5 credits; PRD version generation or major PRD update = 10 credits; PRD challenge / convergence pass = 15 credits. Pack legibility anchors: 100 credits = meaningful first PRD; 200 = deeper iteration; 1000 = power / multi-project. Prices and starter X remain operator-configurable. | **Credit system** sub-component of FG-PRD-V0 gains a directional burn table. Configuration Matrix notes burn rates as product assumptions (labeled as such, not hardcoded). Justifies 100/200/1000 denominations as founder-legible units of value. Partially resolves credit economics gap from challenge. Persist via `/prd converge` → `/prd update`. |

Rules:
- Active queue contains only unresolved questions.
- Answered questions move to Answered.
- Ask one open question at a time.
- Lowest priority number first, then lowest ID.
- Do not keep duplicate open + answered rows for the same question.
