/** Max bundles per preview/export request (aligned with contract zod max). */
export const MAX_BUNDLES_PER_EXPORT = 50;

/** Max total prompt bytes across all tasks in one export. */
export const MAX_TOTAL_PROMPT_BYTES = 5_000_000;
